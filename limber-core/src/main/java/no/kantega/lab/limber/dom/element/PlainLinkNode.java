package no.kantega.lab.limber.dom.element;

import javax.annotation.Nonnull;

public class PlainLinkNode extends LinkNode<PlainLinkNode> {

    public PlainLinkNode(@Nonnull CharSequence tag) {
        super(tag);
    }
}
