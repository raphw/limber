package no.kantega.lab.limber.dom.abstraction;

import no.kantega.lab.limber.dom.element.TextNode;

public interface IDomTextNodeMorphable<M extends IDomTextNodeMorphable<M>> extends IDomNodeMorphable<M, TextNode> {
}
